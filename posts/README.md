# posts/

Empty until the end. When the build is done, we mine the `journal/` entries into a
clean, organized blog series here — likely one post per milestone (see the map in
`STATUS.md`), keeping the real struggles instead of sanding them off.
