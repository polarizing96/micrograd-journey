# code/

The micrograd I write as I go through the video. Starts empty — I'll create files
here as each concept appears (e.g. `value.py` when the `Value` class shows up).

Keeping it scalar-by-scalar like Karpathy does, so every piece is something I typed
and understood, not copied.
